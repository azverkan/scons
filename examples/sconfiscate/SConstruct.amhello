# -*- mode: Python -*-

env = Environment(tools=['default', 'project'])
proj = env.Project('amhello', '', 'maciekp@japhy.fnord.org',
                   header='config.h')
proj.Distribute('configure.ac', 'Makefile.am', 'src/Makefile.am')
proj.AutoInstall(env.Program('src/hello', 'src/main.c'))
